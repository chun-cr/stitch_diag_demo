# Session 013 - 手部扫描无法正常识别修复

## 做了什么
- 排查手部扫描页面、Flutter 状态桥和 Android / iOS 原生手势识别链路，确认页面完成扫描依赖 `readyToScan`。
- 找到根因：原生层已经会输出 `gestureDetected`，但 Flutter 侧 `PalmScanStatus.readyToScan` 只依赖 `handStraight`，导致模型已稳定识别开掌时，页面仍可能永远不开始 2 秒读条。
- 继续下沉修复到 Android / iOS 原生层：手掌扫描放宽 `Open_Palm` 模型分数阈值并降低直掌手指数要求，减少“手掌已摊开但无法稳定通过”的情况。
- 同时移除舌头扫描中“仅凭张嘴与下巴距离就判定通过”的 fallback，改为必须 `tongueOutScore` 达标且嘴部几何有效，避免只露齿就误触发。
- 补充桥接层单测，覆盖事件解析、手掌缺席、`Open_Palm` 强信号兜底，以及舌头扫描对低 `tongueOutScore` 的拒绝逻辑。

## 变更前快照
- `PalmScanStatus.readyToScan` 为 `handPresent && handStraight`。
- Android / iOS 原生手势识别都会上报 `gestureDetected`，但 Flutter 页面并未使用该信号来驱动开始扫描。
- 用户在模型已识别开掌、但几何直掌阈值偏严时，会卡在“已识别手掌但无法完成扫描”的状态。

## 涉及文件
- `lib/features/scan/presentation/services/palm_scan_status_bridge.dart`
- `lib/features/scan/presentation/services/tongue_scan_status_bridge.dart`
- `lib/features/scan/presentation/pages/tongue_scan_page.dart`
- `android/app/src/main/kotlin/com/example/stitch_diag_demo/GestureRecognizerHelper.kt`
- `android/app/src/main/kotlin/com/example/stitch_diag_demo/TongueDetectionUtils.kt`
- `ios/Runner/GestureRecognizerService.swift`
- `ios/Runner/TongueDetectionEvaluator.swift`
- `test/features/scan/palm_scan_status_bridge_test.dart`
- `test/features/scan/tongue_scan_status_bridge_test.dart`

## 问题与决策
- 决策：优先修正 Flutter 侧 readiness 判定，而不是先大改原生阈值；这是最小、最低风险且跨 iOS / Android 一致生效的修复。
- 决策：保留原生 `handStraight` 几何判断作为增强信号，但不再要求它成为唯一完成条件，同时放宽原生开掌判定，减少过严阈值带来的漏检。
- 决策：舌头扫描不再接受“张嘴即通过”的 fallback，避免把牙齿暴露错误当成伸舌动作。
- 决策：通过单测锁住 bridge 层语义，避免未来原生或页面改动再次把强信号或低分数误判放进完成流程。

## 验证结果
- `flutter analyze lib/features/scan/presentation/services/palm_scan_status_bridge.dart lib/features/scan/presentation/pages/palm_scan_page.dart`：通过。
- `flutter analyze lib/features/scan/presentation/services/palm_scan_status_bridge.dart lib/features/scan/presentation/services/tongue_scan_status_bridge.dart lib/features/scan/presentation/pages/palm_scan_page.dart lib/features/scan/presentation/pages/tongue_scan_page.dart`：通过。
- `flutter test test/features/scan/palm_scan_status_bridge_test.dart test/features/scan/tongue_scan_status_bridge_test.dart`：通过。
- `powershell.exe -Command ".\\gradlew.bat app:compileDebugKotlin"`：通过。
- `lsp_diagnostics`：`palm_scan_status_bridge.dart`、`tongue_scan_status_bridge.dart` 与 `tongue_scan_page.dart` 无错误。

## 下次计划
- 真机再回归一轮手掌扫描，重点观察不同距离、不同光照下的完成率是否明显提升。
- 若仍存在少数手势难以触发，可再下探原生阈值（如 `MIN_STRAIGHT_DIGITS`、角度阈值）做第二轮精调。

## 遗留风险
- 当前修复解决的是“模型已识别但页面不开始”的阻塞点；若特定设备上原生识别本身仍不稳定，还需要继续分析相机帧方向与手势阈值。
